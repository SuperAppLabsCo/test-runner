// This is a test to existed endpoint with a existed template

[
  {
    "hardwareConfig": {
      "template": "template_id",
      "endpoint": "endpoint_id"
    },
    "input": {
      "task_id": "task_id",
      "firebase_config": "config",
      "bucket_name": "bucket_name"
    }
  }
]
